# --- Secrets / credentials (NEVER commit) ---
.git-credentials
.gitconfig
.claude.json
.claude/
.bash_history
.ssh/

# --- Large artifacts (exceed GitHub 100MB limit) ---
QGroundControl-installer.exe
QGroundControl-installer.exe.*
QGroundControl.apk
*.apk

# --- Transient build logs ---
qgc-build-resume.log
build_android.log
build_android.run.log
aqtinstall.log

# --- Android build caches dropped into /root ---
.android/
.gradle/

# --- Nested git repo (has its own remote) ---
qgroundcontrol/

# --- System / cache dirs ---
.cache/
.local/
.bashrc
